// Note that ts-node doesn't have to be installed in your project's node modules,
// it may be installed in the quokka global folder. Typescript module is also not required.
// See https://quokkajs.com/docs/configuration.html

import value from './myModule';

console.log(value);